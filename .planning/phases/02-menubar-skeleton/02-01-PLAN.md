---
phase: 02-menubar-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/ChordTyper/ChordTyperApp.swift
  - Tests/ChordTyperTests/MenuStateTests.swift
autonomous: false
requirements:
  - MENU-01
  - MENU-02
  - MENU-03
  - MENU-04

must_haves:
  truths:
    - "D-01, D-02: App uses .menuBarExtraStyle(.menu) dropdown, no .window popover"
    - "D-03, D-04, D-05: Menu layout has Enable ChordTyper toggle, Dictionaries submenu with English/Thai, Settings..., Open Dictionary Folder, Quit ChordTyper in correct order with dividers"
    - "D-06, D-07, D-08: Menubar icon is keyboard.fill when active, keyboard.badge.ellipsis when paused, switches dynamically via @AppStorage"
    - "D-09: No @NSApplicationDelegateAdaptor — pure SwiftUI MenuBarExtra"
    - "D-10, D-11, D-13: @AppStorage keys chordTyperEnabled, englishEnabled, thaiEnabled all default true, persist across restarts"
    - "D-12: All menu items fully wired to state — toggles flip @AppStorage, icon changes, checkmarks toggle"
    - "D-14: Settings... logs to os_log only (placeholder)"
    - "D-15: Open Dictionary Folder opens bundle Resources/dictionaries/ via NSWorkspace"
    - "D-16: Quit ChordTyper calls NSApplication.shared.terminate(nil)"
    - "App launches with no Dock icon and shows a menubar icon (MENU-01)"
  artifacts:
    - path: "Sources/ChordTyper/ChordTyperApp.swift"
      provides: "Full menubar app with icon, toggles, submenu, actions"
      contains: "MenuBarExtra"
    - path: "Tests/ChordTyperTests/MenuStateTests.swift"
      provides: "Unit tests for icon logic and AppStorage defaults"
      contains: "MenuStateTests"
  key_links:
    - from: "Sources/ChordTyper/ChordTyperApp.swift"
      to: "UserDefaults"
      via: "@AppStorage declarations"
      pattern: "@AppStorage.*chordTyperEnabled"
    - from: "Tests/ChordTyperTests/MenuStateTests.swift"
      to: "UserDefaults"
      via: "UserDefaults.standard key assertions"
      pattern: "UserDefaults.standard"
---

<objective>
Evolve the ChordTyperApp.swift stub into a fully wired menubar-only app with dynamic icon states, a complete dropdown menu (enable toggle, dictionaries submenu, Settings stub, Open Dictionary Folder, Quit), and @AppStorage-persisted state. Create unit tests covering icon logic and default state.

Purpose: Deliver the visible UI shell that all later phases (event tap, dictionary, toggle) plug into. Establishes the @AppStorage key interface (chordTyperEnabled, englishEnabled, thaiEnabled) consumed by Phases 3, 5, and 7.

Output: Working menubar app with full menu structure, passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menubar-skeleton/02-CONTEXT.md
@.planning/phases/02-menubar-skeleton/02-RESEARCH.md
@.planning/phases/02-menubar-skeleton/02-PATTERNS.md
@.planning/phases/02-menubar-skeleton/02-UI-SPEC.md

<interfaces>
<!-- Current ChordTyperApp.swift stub (the starting point to evolve): -->
From Sources/ChordTyper/ChordTyperApp.swift:
- @main struct ChordTyperApp: App
- body: MenuBarExtra("ChordTyper", systemImage: "keyboard") with .menuBarExtraStyle(.menu)
- Imports: SwiftUI only

From Tests/ChordTyperTests/ChordTyperSmokeTests.swift:
- import XCTest
- final class ChordTyperSmokeTests: XCTestCase
- Test naming: test[Component]_[scenario]_[expectedBehavior]

From project.yml:
- Test target: ChordTyperTests (bundle.unit-test, depends on ChordTyper)
- SWIFT_STRICT_CONCURRENCY: complete
- Bundle ID: dev.chutipon.chordtyper
- Resources path: Resources/dictionaries (copied into app bundle)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MenuStateTests and evolve ChordTyperApp with full menu structure</name>
  <files>Tests/ChordTyperTests/MenuStateTests.swift, Sources/ChordTyper/ChordTyperApp.swift</files>
  <read_first>
    - Sources/ChordTyper/ChordTyperApp.swift
    - Tests/ChordTyperTests/ChordTyperSmokeTests.swift
    - .planning/phases/02-menubar-skeleton/02-PATTERNS.md
    - .planning/phases/02-menubar-skeleton/02-CONTEXT.md
  </read_first>
  <behavior>
    - testMenuState_menuBarIcon_activeShouldBeKeyboardFill: iconName(for: true) returns "keyboard.fill"
    - testMenuState_menuBarIcon_pausedShouldBeKeyboardBadgeEllipsis: iconName(for: false) returns "keyboard.badge.ellipsis"
    - testMenuState_defaultChordTyperEnabled_noStoredValueOnFreshKey: UserDefaults.standard.object(forKey: "chordTyperEnabled") is nil after removing the key (proving @AppStorage default governs)
    - testMenuState_defaultEnglishEnabled_noStoredValueOnFreshKey: same for "englishEnabled"
    - testMenuState_defaultThaiEnabled_noStoredValueOnFreshKey: same for "thaiEnabled"
    - testMenuState_toggleChordTyperEnabled_persistsToUserDefaults: setting UserDefaults.standard.set(false, forKey: "chordTyperEnabled") then reading back returns false (proves UserDefaults round-trip works)
  </behavior>
  <action>
    RED phase: Create Tests/ChordTyperTests/MenuStateTests.swift with the six tests listed in behavior block. Follow the analog pattern from ChordTyperSmokeTests.swift (import XCTest, final class, test naming convention). Include setUp() that calls UserDefaults.standard.removeObject(forKey:) for all three keys: "chordTyperEnabled", "englishEnabled", "thaiEnabled". Include a private helper func iconName(for enabled: Bool) -> String that mirrors the production logic: enabled ? "keyboard.fill" : "keyboard.badge.ellipsis". Run make test -- tests must fail or pass trivially (icon helper is self-contained so those pass; UserDefaults assertions should pass since they test absence).

    GREEN phase: Evolve Sources/ChordTyper/ChordTyperApp.swift to the full menu structure per D-01 through D-16:
    - Add imports: AppKit, os (keep SwiftUI)
    - Add file-scope logger: Logger(subsystem: "dev.chutipon.chordtyper", category: "AppMenu")
    - Add three @AppStorage properties with explicit = true defaults: chordTyperEnabled, englishEnabled, thaiEnabled (per D-10, D-11, D-13)
    - Add computed property menuBarIcon: String returning "keyboard.fill" when chordTyperEnabled is true, "keyboard.badge.ellipsis" when false (per D-06, D-07, D-08)
    - Replace MenuBarExtra body content with: Toggle("Enable ChordTyper", isOn: $chordTyperEnabled) per D-05, Divider, Menu("Dictionaries") containing Toggle("English", isOn: $englishEnabled) and Toggle("Thai", isOn: $thaiEnabled) per D-03, Divider, Button("Settings...") that calls logger.info per D-14, Button("Open Dictionary Folder") that uses Bundle.main.resourceURL?.appendingPathComponent("dictionaries") with optional chaining and NSWorkspace.shared.open(url) per D-15, Divider, Button("Quit ChordTyper") that calls NSApplication.shared.terminate(nil) per D-16
    - Pass menuBarIcon to MenuBarExtra systemImage parameter
    - Keep .menuBarExtraStyle(.menu) per D-01
    - Do NOT apply .toggleStyle() on any Toggle (breaks checkmark rendering)
    - Do NOT add .padding() or .frame() on menu items (breaks native NSMenu appearance)
    - Do NOT add @NSApplicationDelegateAdaptor per D-09

    Run make build && make test -- both must pass.
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/chord-typer && make build && make test</automated>
  </verify>
  <acceptance_criteria>
    - Sources/ChordTyper/ChordTyperApp.swift contains `import AppKit` and `import os`
    - Sources/ChordTyper/ChordTyperApp.swift contains `@AppStorage("chordTyperEnabled") private var chordTyperEnabled: Bool = true`
    - Sources/ChordTyper/ChordTyperApp.swift contains `@AppStorage("englishEnabled") private var englishEnabled: Bool = true`
    - Sources/ChordTyper/ChordTyperApp.swift contains `@AppStorage("thaiEnabled") private var thaiEnabled: Bool = true`
    - Sources/ChordTyper/ChordTyperApp.swift contains `"keyboard.fill"` and `"keyboard.badge.ellipsis"` in a computed property
    - Sources/ChordTyper/ChordTyperApp.swift contains `Toggle("Enable ChordTyper"` and `Menu("Dictionaries")` and `Button("Quit ChordTyper")`
    - Sources/ChordTyper/ChordTyperApp.swift contains `Button("Settings...")` with logger.info call (no print())
    - Sources/ChordTyper/ChordTyperApp.swift contains `Bundle.main.resourceURL?.appendingPathComponent("dictionaries")` (no force unwrap)
    - Sources/ChordTyper/ChordTyperApp.swift does NOT contain `@NSApplicationDelegateAdaptor`
    - Sources/ChordTyper/ChordTyperApp.swift does NOT contain `.toggleStyle(`
    - Tests/ChordTyperTests/MenuStateTests.swift exists with final class MenuStateTests: XCTestCase
    - Tests/ChordTyperTests/MenuStateTests.swift contains setUp() removing all three UserDefaults keys
    - Tests/ChordTyperTests/MenuStateTests.swift contains at least 5 test methods matching test naming convention
    - `make build` exits 0
    - `make test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>ChordTyperApp.swift has full menu structure per all D-01 through D-16 decisions. MenuStateTests.swift has 6 tests covering icon logic and AppStorage defaults. make build && make test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of menubar app</name>
  <files>Sources/ChordTyper/ChordTyperApp.swift</files>
  <read_first>
    - Sources/ChordTyper/ChordTyperApp.swift
    - .planning/phases/02-menubar-skeleton/02-UI-SPEC.md
  </read_first>
  <what-built>Complete menubar skeleton with dynamic icon, full dropdown menu (toggle, dictionaries submenu, Settings stub, Open Dictionary Folder, Quit), and persisted state.</what-built>
  <action>
    Run make run to launch the app. Visually inspect the menubar icon, dropdown menu structure, toggle behavior, icon state switching, Dictionaries submenu, Open Dictionary Folder action, and Quit action. Verify state persistence by quitting and relaunching.
  </action>
  <how-to-verify>
    1. Run `make run` from the project root
    2. Verify NO Dock icon appears for ChordTyper (MENU-01)
    3. Verify menubar shows a keyboard.fill icon (solid keyboard) (MENU-02)
    4. Click the menubar icon -- verify dropdown shows:
       - "Enable ChordTyper" with a checkmark (MENU-03)
       - A divider
       - "Dictionaries >" submenu arrow (MENU-03)
       - A divider
       - "Settings..." (MENU-03)
       - "Open Dictionary Folder" (MENU-03)
       - A divider
       - "Quit ChordTyper" (MENU-04)
    5. Click "Enable ChordTyper" to uncheck it -- verify the menubar icon changes to keyboard.badge.ellipsis (MENU-02)
    6. Click it again to re-enable -- verify icon returns to keyboard.fill
    7. Hover over "Dictionaries >" -- verify submenu shows "English" (checked) and "Thai" (checked)
    8. Click "English" to uncheck it, then reopen to verify it stays unchecked
    9. Click "Open Dictionary Folder" -- verify Finder opens showing the dictionaries folder
    10. Click "Quit ChordTyper" -- verify the app terminates (MENU-04)
    11. Run `make run` again -- verify the toggle states you changed in step 6/8 persisted (D-10, D-11)
  </how-to-verify>
  <verify>
    <human-check>Visual inspection of menubar icon, dropdown menu, toggle behavior, state persistence, and quit action per steps 1-11 above</human-check>
  </verify>
  <acceptance_criteria>
    - No Dock icon visible when app is running
    - Menubar icon shows keyboard.fill (active) and keyboard.badge.ellipsis (paused)
    - All menu items present with correct labels matching UI-SPEC exactly
    - Toggle checkmarks update on click
    - Dictionaries submenu renders as hierarchical submenu with disclosure arrow
    - Open Dictionary Folder opens Finder to the dictionaries directory
    - Quit terminates the app
    - Toggle states persist across restart
  </acceptance_criteria>
  <done>User has visually confirmed all menubar behaviors match the UI-SPEC contract.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this phase. The menubar skeleton has no external input processing, no network calls, no credential handling, and no event interception. The only data persisted is three boolean UserDefaults keys (chordTyperEnabled, englishEnabled, thaiEnabled) -- no user-controlled input beyond clicking native menu items.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | UserDefaults (3 boolean keys) | accept | Three booleans (enabled/disabled flags) contain no PII or secrets. UserDefaults is app-sandboxed by default on macOS. Acceptable risk. |

No package installs in this phase -- Package Legitimacy Gate not applicable.
</threat_model>

<verification>
1. `make build` exits 0 (project compiles with evolved ChordTyperApp.swift)
2. `make test` exits 0 (all MenuStateTests + existing smoke test pass)
3. Visual inspection confirms menubar icon, dropdown menu structure, toggle persistence, and quit behavior
</verification>

<success_criteria>
- App launches as menubar-only with no Dock icon (MENU-01)
- Icon shows keyboard.fill when active, keyboard.badge.ellipsis when paused (MENU-02)
- Dropdown menu matches UI-SPEC layout exactly: toggle, dictionaries submenu, Settings, Open Dictionary Folder, Quit (MENU-03)
- Quit terminates the app cleanly (MENU-04)
- All six MenuStateTests pass
- make build && make test green
</success_criteria>

<output>
Create `.planning/phases/02-menubar-skeleton/02-01-SUMMARY.md` when done
</output>
