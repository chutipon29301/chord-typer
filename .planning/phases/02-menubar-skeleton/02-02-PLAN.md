---
phase: 02-menubar-skeleton
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - Resources/dictionaries/english.json
  - Resources/dictionaries/thai.json
  - Sources/ChordTyper/ChordTyperApp.swift
  - Tests/ChordTyperTests/ChordTyperAppTests.swift
autonomous: true
requirements: [MENU-03]
gap_closure: true
closes_gaps:
  - "UAT test 6: Open Dictionary Folder silently fails because dictionaries folder is empty (.gitkeep only) and not copied into bundle"

must_haves:
  truths:
    - "Clicking 'Open Dictionary Folder' opens Finder to the dictionaries folder inside the app bundle"
    - "If dictionaries folder is missing from bundle, a warning is logged via os.log"
  artifacts:
    - path: "Resources/dictionaries/english.json"
      provides: "Placeholder English dictionary so folder gets copied into bundle"
      contains: "eht"
    - path: "Resources/dictionaries/thai.json"
      provides: "Placeholder Thai dictionary so folder gets copied into bundle"
      contains: "{}"
    - path: "Sources/ChordTyper/ChordTyperApp.swift"
      provides: "Guard on dictionaries URL existence with os.log warning"
      contains: "FileManager"
  key_links:
    - from: "project.yml resources"
      to: "Resources/dictionaries/"
      via: "xcodebuild copy resources phase"
      pattern: "path: Resources/dictionaries"
---

<objective>
Fix UAT test 6: "Open Dictionary Folder" does nothing because the dictionaries folder contains only .gitkeep, so xcodebuild never copies it into the app bundle. The NSWorkspace.shared.open(url) call silently fails on the non-existent path.

Purpose: Close the single remaining gap from Phase 02 UAT so the phase passes fully.
Output: Placeholder dictionary JSON files that ensure the folder exists in the bundle, and a guard that logs a warning if the path is missing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menubar-skeleton/02-01-SUMMARY.md
@.planning/phases/02-menubar-skeleton/02-UAT.md

@Sources/ChordTyper/ChordTyperApp.swift
@project.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add placeholder dictionary files</name>
  <files>Resources/dictionaries/english.json, Resources/dictionaries/thai.json</files>
  <action>
Create two minimal JSON dictionary files so xcodebuild copies the dictionaries folder into the app bundle:

english.json: A valid JSON object with one sample chord entry: key "eht" maps to value "the". This follows the sorted-alphabetical key encoding convention (t+h+e sorted = eht). Phase 5 will replace this with the full 150-entry dictionary.

thai.json: An empty JSON object (just `{}`). Phase 5 will populate this with 100+ Thai entries.

Remove the .gitkeep file since real files now exist in the folder.

These files ensure `Resources/dictionaries` is non-empty and xcodebuild's "Copy Bundle Resources" phase copies the folder into ChordTyper.app/Contents/Resources/dictionaries/.
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/chord-typer && test -f Resources/dictionaries/english.json && test -f Resources/dictionaries/thai.json && python3 -c "import json; json.load(open('Resources/dictionaries/english.json')); json.load(open('Resources/dictionaries/thai.json'))" && echo "PASS"</automated>
  </verify>
  <done>english.json contains valid JSON with at least one chord entry, thai.json contains valid empty JSON object, .gitkeep removed</done>
</task>

<task type="auto">
  <name>Task 2: Guard dictionary folder existence and add test</name>
  <files>Sources/ChordTyper/ChordTyperApp.swift, Tests/ChordTyperTests/ChordTyperAppTests.swift</files>
  <action>
In ChordTyperApp.swift, update the "Open Dictionary Folder" button action to check whether the dictionaries directory actually exists on disk before calling NSWorkspace.shared.open(url). Use FileManager.default.fileExists(atPath: url.path) after constructing the URL. If the URL is nil (resourceURL missing), keep the existing logger.error call. If the URL is constructed but the directory does not exist on disk, log a warning via logger.warning including the attempted path, so the failure is visible in Console.app. Only call NSWorkspace.shared.open(url) when the directory confirmed exists.

In ChordTyperTests, add a unit test that verifies Bundle.main (the test host bundle) can locate the dictionaries resource folder. The test should confirm that english.json and thai.json are valid JSON by loading and decoding them. Name the test: testOpenDictionaryFolder_bundleContainsDictionaries_filesAreValidJSON.

Run make build && make test to confirm everything passes.
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/chord-typer && make build && make test</automated>
  </verify>
  <done>The "Open Dictionary Folder" button opens Finder to the dictionaries folder in the app bundle. If the folder is missing, a warning is logged via os.log. Unit test confirms dictionary files are bundled and valid JSON. make build && make test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this plan adds static JSON resource files and a filesystem existence check.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Resources/dictionaries/*.json | accept | Dictionary files are bundled read-only inside .app; ad-hoc signed. Phase 5 will add validation when loading dictionaries at runtime. |
</threat_model>

<verification>
1. `make build && make test` passes
2. `make run` — click "Open Dictionary Folder" — Finder opens to the dictionaries folder containing english.json and thai.json
</verification>

<success_criteria>
- Clicking "Open Dictionary Folder" in the menubar dropdown opens Finder to the correct dictionaries folder inside the app bundle
- english.json and thai.json exist in the built app bundle at Contents/Resources/dictionaries/
- If dictionaries folder is somehow missing, a warning appears in Console.app logs (not a silent failure)
- `make build && make test` passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/02-menubar-skeleton/02-02-SUMMARY.md` when done
</output>
