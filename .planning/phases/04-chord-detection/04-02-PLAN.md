---
phase: 04-chord-detection
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: false
requirements: [CHRD-01, CHRD-02, CHRD-03, CHRD-04, CHRD-05]

must_haves:
  truths:
    - "Chord detection is live in the running app (eventHandler is wired)"
    - "Keystrokes pass through normally when fewer than 3 keys pressed"
    - "No app freeze or deadlock occurs during normal typing"
  artifacts: []
  key_links: []
---

<objective>
Verify that ChordEngine is correctly integrated into the live app — keystrokes pass through transparently, no deadlocks occur during typing, and the app remains responsive.

Purpose: ChordEngine's replay mechanism has a critical deadlock risk (Pitfall 1 from RESEARCH.md). Human verification confirms the isReplaying guard works correctly in the real CGEventTap callback environment, not just in unit tests.
Output: Human confirmation that the app works correctly with chord detection active.
</objective>

## Phase Goal

**As a** Mac user, **I want to** press 3+ keys simultaneously and have ChordTyper detect the chord, **so that** the chord key can be looked up in a dictionary for word output.

<execution_context>
@/Users/chutipon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chutipon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-chord-detection/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify chord detection integration in live app</name>
  <what-built>ChordEngine state machine wired into EventTapManager via AppDelegate. All keyDown/keyUp events now pass through ChordEngine.process() before reaching the target app. Since chordLookup defaults to { _ in nil }, all chords should result in key replay (no matches). Normal typing (1-2 keys) should pass through unchanged.</what-built>
  <how-to-verify>
    1. Run `make run` from the project root
    2. Grant Accessibility permission if prompted (System Settings > Privacy & Security > Accessibility)
    3. Open TextEdit and type normally — single characters should appear without delay or duplication
    4. Type quickly (fast sequential keys) — no characters should be lost or duplicated
    5. Try pressing 3 keys simultaneously (e.g., hold T+H+E then release) — since no dictionary is loaded, the keys should replay and appear in TextEdit (may appear in a different order than pressed, but no keys should be lost)
    6. Try Cmd+C, Cmd+V, Cmd+Z — system shortcuts should work normally (modifier abort)
    7. Keep typing for 30+ seconds — no app freeze or beachball should occur (deadlock test)
    8. Check Console.app filtered to "ChordTyper" — look for ChordEngine log messages (.notice level)
    9. Quit ChordTyper from the menubar
  </how-to-verify>
  <resume-signal>Type "approved" if typing works normally with no freezes, or describe any issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only verifies — no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Verification-only plan; threats addressed in 04-01 |
</threat_model>

<verification>
Human confirms:
1. Normal typing works without delay or duplication
2. System shortcuts (Cmd+C/V/Z) work correctly
3. No app freeze during 30+ seconds of typing
4. ChordEngine log messages visible in Console.app
</verification>

<success_criteria>
- Human approves that typing is transparent and responsive
- No deadlock observed during extended typing session
- Modifier shortcuts work without interference
</success_criteria>

<output>
Create `.planning/phases/04-chord-detection/04-02-SUMMARY.md` when done
</output>
